glossary = {
    'print': 'displays the text in parenthesis',
    'new line': 'breaks text onto new line',
    'range': 'creates a range of values that starts at the first number and ends one before the second number.',
    'list': 'a group of values under one variable.',
    'tuple': 'a list that cant be edited',
    'for loop': 'a loop that runs until it cant anymore',
    'dictionary': ' a list with keys and values',
    'if-else': 'a chain that runs based on wether conditions are met',
    'append': 'adds a value to the end of a list',
    'insert': 'adds a value to a specified point in a list',
}

for term, definition in glossary.items():
    print(f'\n{term}: {definition}')